<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.31.xsd">

    <changeSet id="002" author="finstream">
        <createTable tableName="notification_preferences">
            <column name="id" type="uuid">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="account_id" type="varchar(100)">
                <constraints nullable="false"/>
            </column>
            <column name="notification_type" type="varchar(50)">
                <constraints nullable="false"/>
            </column>
            <column name="channel" type="varchar(20)">
                <constraints nullable="false"/>
            </column>
            <column name="enabled" type="boolean" defaultValueBoolean="true">
                <constraints nullable="false"/>
            </column>
            <column name="destination" type="varchar(255)"/>
        </createTable>

        <addUniqueConstraint
                tableName="notification_preferences"
                columnNames="account_id, notification_type, channel"
                constraintName="uk_account_type_channel"/>

        <createIndex tableName="notification_preferences" indexName="idx_account_id">
            <column name="account_id"/>
        </createIndex>
    </changeSet>
</databaseChangeLog>